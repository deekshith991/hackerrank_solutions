

/*
	TITLE : 

	LINK  : 

	DESCRIPTION : 
			Here a string is given as input we need to remove letters such that
			the formed dtring would be altenating i.e., abababab and we return the length
			of that string but if we remove a letter we  need to remove all instances of that letter
*/

#include <bits/stdc++.h>
#include <string.h>
using namespace std;

int alternate(string s){

	vector<char> str(s.begin(),s.end());

	for(int i=0 ; i < str.size() ; i++ )

	return 0;
}

char duplicate(vector<char> str){
	
}

int main(){

	string s = "abaabacdacad";

	alternate(s);

	
}
